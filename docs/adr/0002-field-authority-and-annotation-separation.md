# Field authority and annotation separation

Operational facts are resolved by field-specific source authority and freshness, while human names, descriptions, ownership, tags and publication choices live in annotations. This prevents scans from overwriting intent and prevents stale or inferred values from masquerading as provider truth.
