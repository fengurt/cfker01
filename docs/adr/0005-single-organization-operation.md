# Single-organization operation

The deployment operates one default organization for the current period, while every Task, Agent key and permission query remains organization-scoped. This preserves isolation without adding self-service multi-tenant product complexity before there is a real second organization.
