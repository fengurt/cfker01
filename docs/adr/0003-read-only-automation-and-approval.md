# Read-only automation and approval

Scanners and Agents may discover, recommend and write scoped internal collaboration records, but external infrastructure remains read-only by default. Any future external mutation must be explicitly approved, idempotent, auditable and reversible.
